@model IEnumerable<Kinetics.Models.JobPosition>

@{
    ViewData["Title"] = "Index";
}

<!-- Include shared header -->
@await Html.PartialAsync("_SharedTop")
<h2>Positions</h2>
<p>
    <a asp-action="Create" class="btn btn-primary">Add New Position</a>
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>S/N</th>
            <th>
                @Html.DisplayNameFor(model => model.PositionName)
            </th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @{
            var i = 0;
        }
        @foreach (var item in Model)
        {
            i = i + 1;
        <tr>
            <td>@i</td>
            <td>
                @Html.DisplayFor(modelItem => item.PositionName)
            </td>
            <td class="vc-table-action">
                <a asp-action="Edit" asp-route-id="@item.JobPositionID" class="btn btn-primary">Edit</a>
                <a asp-action="Details" asp-route-id="@item.JobPositionID" class="btn btn-info">Details</a>
                <a asp-action="Delete" asp-route-id="@item.JobPositionID" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<!-- Include shared bottom -->
@await Html.PartialAsync("_SharedBottom")